<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Compose draws the real UI; this base theme just configures the splash /
         window background and status-bar handling before Compose takes over. -->
    <style name="Theme.SeedSync" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <!-- System-bar colors/icon contrast are handled at runtime by
             enableEdgeToEdge() (auto light/dark). The deprecated statusBarColor /
             windowLightStatusBar attrs are ignored on Android 15 and would fight
             it, so they're intentionally omitted. -->
    </style>
</resources>
